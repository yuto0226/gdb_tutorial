#include <stdio.h>

int main() {
    int inputNum

        printf("請輸入一個整數: ");
    scanf("%d", &inputNum)

    if(inputNum < 0 {
        printf("數字是負的\n");
    } else {
        printf("數字不是負的\n");

        print("你輸入的是: %d\n", inputNum);

        return 0
}
